# Data

This repo is designed to be reproducible without committing proprietary data.

For the job-simulation datasets included here, place files in:

- `data/raw/Nat_Gas.csv`
- `data/raw/Loan_Data.csv`

If you have your own data sources, keep raw files out of git and update the paths in `configs/` or when constructing the model objects.
